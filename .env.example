# Thordata Proxy Examples - Minimal .env.example
# Copy to .env and fill with your real credentials. Do NOT commit .env.

# Required: Scraper token (Dashboard -> Account Settings)
THORDATA_SCRAPER_TOKEN=your_scraper_token_here

# Required: Residential proxy sub-user credentials
THORDATA_RESIDENTIAL_USERNAME=your_residential_username_here
THORDATA_RESIDENTIAL_PASSWORD=your_residential_password_here

# Proxy endpoint (Dashboard -> Residential Proxies -> Endpoint Generator)
# For many accounts, this may be a dedicated HTTPS endpoint.
THORDATA_PROXY_HOST=vpnXXXX.pr.thordata.net
THORDATA_PROXY_PORT=9999

# Optional: Upstream proxy when behind GFW/firewall (Clash, corporate proxy, etc.)
# Matches the behavior in thordata-python-sdk: if set, the SDK will tunnel
# via this upstream before reaching Thordata.
# Examples:
#   HTTP:  THORDATA_UPSTREAM_PROXY=http://127.0.0.1:7897
#   SOCKS: THORDATA_UPSTREAM_PROXY=socks5://127.0.0.1:7898
# THORDATA_UPSTREAM_PROXY=
